<UserControl x:Class="PortableJournal.View.NewScreenControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PortableJournal.View"
             xmlns:vm="clr-namespace:PortableJournal.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <vm:ViewModelNewScreen x:Key="ViewModelNewScreen"/>

        <Style TargetType="Button">
            <Setter Property="Height" Value="50"/>
            <Setter Property="Width" Value="150"/>
            <Setter Property="Background" Value="AntiqueWhite"/>
            <Setter Property="Foreground" Value="DarkGreen"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontFamily" Value="Arial"/>
            <Setter Property="FontSize" Value="16"/>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" 
               Margin="0,100,0,0"
               HorizontalAlignment="Center"
               Content="Welcome to Portable Journal!  What would you like to do?"
               FontSize="40"
               FontWeight="Bold"
               FontFamily="Arial"
               Foreground="DarkGreen"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <!--wrap the 2 columns I actually need with star columns to align in middle.  There's probably a better way-->
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Grid.Column="1" Margin="100">
                <Button Command="{Binding DataContext.ShowJournalView, RelativeSource={RelativeSource AncestorType={x:Type Window}}, Mode=OneWay}">
                    <TextBlock Text="Create Journal" TextWrapping="Wrap" HorizontalAlignment="Center" />
                </Button>
            </StackPanel>
            <StackPanel Grid.Row="0" Grid.Column="2" Margin="100">
                <Button>
                    <TextBlock Text="Open Journal" TextWrapping="Wrap" HorizontalAlignment="Center"/>
                </Button>
            </StackPanel>
            <TextBox Grid.Row="1" Text="{Binding NewJournalName}"/>
            <Button Content="Add Journal to DB" Command="{Binding AddJournalCommand}"/>
        </Grid>
        <ItemsControl Grid.Row="2" ItemsSource="{Binding ExistingJournals}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding Name}" Command="{Binding OpenJournalCommand}" CommandParameter="{Binding Name}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
