<Window x:Class="PortableJournal.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:PortableJournal.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" 
        WindowState="Maximized"
        DataContext="{DynamicResource ViewModelMain}">
    <Window.Resources>
        <vm:ViewModelMain x:Key="ViewModelMain"/>
    </Window.Resources>

    <Window.InputBindings>
        <KeyBinding Key="O" Modifiers="Control" Command="{Binding OpenCommand}"/>
        <KeyBinding Key="S" Modifiers="Control" Command="{Binding SaveCommand}"/>
    </Window.InputBindings>

    <!-- Just a note: this layout looks okay for what I have so far (which is minimal), but it's a hacky solution and it doesn't really match what I want
        long term.  Just pointing this out, because you're going to have to figure out how to align shit eventually, pal.  -->
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_New"/>
                <MenuItem Header="_Open" Command="{Binding OpenCommand}"/>
                <MenuItem Header="_Save" Command="{Binding SaveCommand}"/>
                <MenuItem Header="_Close" Command="{Binding CloseCommand}"/>
                <MenuItem Header="E_xit" Command="{Binding ExitCommand}"/>
            </MenuItem>
        </Menu>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <Label>Journal Entry Length:</Label>
            <Label Content="{Binding EntryLength}"/>
        </StackPanel>
        <TextBox DockPanel.Dock="Bottom" VerticalAlignment="Stretch" AcceptsReturn="True" Text="{Binding EntryText, UpdateSourceTrigger=PropertyChanged}"/>
    </DockPanel>
</Window>
